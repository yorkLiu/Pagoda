<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE urlrewrite PUBLIC "-//tuckey.org//DTD UrlRewrite 3.0//EN"
    "http://tuckey.org/res/dtds/urlrewrite3.0.dtd">

<urlrewrite>
  <!--<rule>-->
    <!--<from>^/html/wallpapers/(.*)$</from>-->
    <!--<to type="forward">/scripts/desktop/wallpapers/$1</to>-->
  <!--</rule>-->
  <outbound-rule encodefirst="true">
    <note>Remove jsessionid from embedded urls - for urls WITH query parameters</note>
    <from>^/(.*);jsessionid=.*[?](.*)$</from>
    <to encode="false">/$1?$2</to>
  </outbound-rule>

  <outbound-rule encodefirst="true">
    <note>Remove jsessionid from embedded urls - for urls WITHOUT query parameters</note>
    <from>^/(.*);jsessionid=.*[^?]$</from>
    <to encode="false">/$1</to>
  </outbound-rule>
  <!--<rule>-->
    <!--<from>^/admin/user/(.*)\.html$</from>-->
    <!--<to type="forward">/admin/userform.html?id=$1&amp;from=list</to>-->
  <!--</rule>-->
    <!-- The following fixes a bug in XFire:        -->
    <!-- http://jira.codehaus.org/browse/XFIRE-1089 -->
  <!--<rule>-->
    <!--<from>^//services/(.*)$</from>-->
    <!--<to type="forward">/services/$1</to>-->
  <!--</rule>-->
</urlrewrite>

